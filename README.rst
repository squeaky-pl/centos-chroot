Scripts for CentOS chroot
=========================

This repository contains two scripts to `create` and `enter` CentOS chroot
on deb & rpm based Linuxes. Creation is configurable with `chroot.spec.xml` file.
